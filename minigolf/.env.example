# Pusher Configuration (Required for multiplayer)
# Create a free account at https://pusher.com and get your credentials

NEXT_PUBLIC_PUSHER_KEY=your_pusher_key
NEXT_PUBLIC_PUSHER_CLUSTER=mt1
PUSHER_APP_ID=your_app_id
PUSHER_SECRET=your_pusher_secret

# Note: Without Pusher credentials, the game will work in single-player/local mode
# Each player will have their own game state (great for testing!)
