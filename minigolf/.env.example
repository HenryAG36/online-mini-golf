# Pusher Configuration (Optional - for online multiplayer)
# Get your keys from https://pusher.com
PUSHER_APP_ID=your_app_id
PUSHER_SECRET=your_secret
NEXT_PUBLIC_PUSHER_KEY=your_key
NEXT_PUBLIC_PUSHER_CLUSTER=us2

# Note: The game works without Pusher for local/same-device play
# using BroadcastChannel API. Pusher is only needed for remote multiplayer.
